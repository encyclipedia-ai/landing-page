# Closed-beta invite form. Each "Request an invite" CTA opens this URL in a
# new tab. Use a Google Form (recommended), Typeform, or any link.
#
# If unset, CTAs fall back to a pre-filled mailto: to mau@encyclipedia.ai so
# the site is still functional during local dev / before the form is wired.
NEXT_PUBLIC_INVITE_FORM_URL=https://forms.gle/TqmoQowcGULhtTLL6
